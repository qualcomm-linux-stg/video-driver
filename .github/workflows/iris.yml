name: IRIS App Injection

on:
  workflow_dispatch:
  push:
    branches:
      - test-workflow

jobs:
  inject:
    runs-on: self-hosted
    strategy:
      fail-fast: false
      matrix:
        machines: [qcs9100-ride-sx, qcs8300-ride-sx, qcs6490-rb3gen2-core-kit]

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Copy all image files to workspace
        run: |
          echo "📦 Copying all image files for ${{ matrix.machines }}..."

          SRC_DIR=/local/mnt/workspace/manigurr/images/images/${{ matrix.machines }}
          DEST_DIR=${{ github.workspace }}/images/${{ matrix.machines }}

          if [[ ! -d "$SRC_DIR" ]]; then
            echo "❌ Source directory not found: $SRC_DIR"
            ls /local/mnt/workspace/manigurr/images/images || echo "🔍 Available directories
